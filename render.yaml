services:
  - type: web
    name: euroace-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: PORT
        value: 3005
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
